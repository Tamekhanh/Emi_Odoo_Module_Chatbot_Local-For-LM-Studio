<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action cho giao diện Chat Bubble (OWL) -->
    <record id="action_emi_chat_client" model="ir.actions.client">
        <field name="name">Chat với Emi</field>
        <field name="tag">emi_chat_action</field>
    </record>

    <!-- Menu để mở Chat -->
    <menuitem id="menu_emi_chat" 
              name="Chat" 
              parent="menu_emi_root" 
              action="action_emi_chat_client" 
              sequence="20"/>
</odoo>